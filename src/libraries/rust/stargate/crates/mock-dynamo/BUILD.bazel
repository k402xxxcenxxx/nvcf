# SPDX-FileCopyrightText: Copyright (c) NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

# Test/dev helper used by integration tests. Not packaged into an OCI
# image (matches the upstream Dockerfile which builds it as a debug
# helper only).

load("@stargate_crates//:defs.bzl", "aliases", "all_crate_deps")
load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "mock-dynamo",
    srcs = glob(["src/**/*.rs"]),
    aliases = aliases(),
    edition = "2024",
    proc_macro_deps = all_crate_deps(proc_macro = True),
    visibility = ["//visibility:public"],
    deps = [
        "//src/libraries/rust/stargate/crates/proto:stargate-proto",
    ] + all_crate_deps(normal = True),
)
